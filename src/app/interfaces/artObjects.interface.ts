export interface ArtObjects {
  objectNumber: string;
  title: string;
  principalOrFirstMaker: string;
  longTitle: string;
  webImage: {
    url: string;
  };
  headerImage: {
    url: string;
  };
  hasImage: boolean;
}
